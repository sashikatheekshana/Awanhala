class SelectedFood {
  String name;
  String imageURL;
  int qty;
  int available;
  double unitPrice;
  double itemTotal;

  SelectedFood(this.name, this.imageURL, this.qty, this.available,
      this.unitPrice, this.itemTotal);
}
